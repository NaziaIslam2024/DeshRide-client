import React from 'react';

const Card = () => {
    const cardsData = [

    ]
    return (
        <div>
            {
                cardsData.map(card =>{
                     
                })
            }
        </div>
    );
};

export default Card;